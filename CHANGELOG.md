# 版本记录

## v2.0.0 — 2026-09-20

本次迭代在 v1.1.1 之后引入不兼容的 API 调整，因此升级主版本号；同步修正此前源码版本号滞留在 1.0.0 的问题。

### 新增与优化

- 支持 Python / JavaScript 脚本签到、编辑示例和历史脚本预览。
- 手动与定时任务统一进入 SQLite 持久化有界队列，按账号去重；随机等待、失败重试不占用执行线程。
- HTTP 响应流式限量读取，限制压缩响应、重定向及慢速传输；脚本增加超时、输出和进程树资源监控，支持 Linux cgroup v2 配额。
- 通知按渠道独立入队及重试，通知失败不重复执行签到。
- SQLite WAL、显式索引迁移、分页与字段裁剪、详情按需加载、分批日志清理和流式导出。
- 使用单进程 Waitress 和数据目录进程锁；Compose 默认 1 CPU / 640MiB，配置日志轮转。

### 不兼容调整

| 原调用方式 | v2.0.0 调整 |
|---|---|
| `POST /api/checkin/<id>` 等待签到完成 | 返回 HTTP 202 和 `data.id`；使用 `GET /api/executions/<id>` 查询结果。队列满返回 429。 |
| `GET /api/accounts` 返回全部账号和正文 | 改为分页摘要；使用 `GET /api/accounts/<id>` 获取完整配置，`page_size` 最大 100。 |
| 日志列表返回完整响应正文 | 列表只返回摘要；使用 `GET /api/logs/<id>/response` 获取已保存的完整响应。 |
| 导入 `src.app` 即启动数据库迁移和调度器 | 统一使用 `python run.py` 显式启动，不能用多实例共用数据目录。 |
| 单请求批量导入不设上限 | 每次最多 100 个账号，请求正文最大 2MiB；页面自动拆分批次。 |

### 升级方式

1. 正常停止旧服务，备份整个数据目录。使用旧版程序回退时，应恢复升级前备份。
2. 安装新依赖或重建镜像，通过 `python run.py` 或仓库 Compose 启动；启动时自动迁移旧数据库。
3. 已有数据库保留原配置，在系统设置中确认日志自动清理开启，建议保留 500～1000 条。
4. 外部 API 调用方按上表适配。执行中崩溃的任务标记为中断，请先确认外部操作结果，再决定是否重试。

### 验证

- Linux：36 项测试全部通过；Windows：33 项通过，3 项 Linux 专用测试跳过。
- 浏览器验证分页、脚本编辑、异步执行状态和日志详情通过。
- 单 CPU 模拟负载：24 个任务全部成功，进程树采样峰值 RSS 69.8MiB，列表接口中位延迟 4.22ms。
- 尚未实际构建 Docker 镜像或在目标 1G 内存限制下验收。完整测量方法及边界见 [性能验证记录](docs/performance.md)。
